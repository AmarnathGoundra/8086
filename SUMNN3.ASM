ASSUME CS: CODE, DS: DATA
DATA SEGMENT
COUNT DB 04H
RES DW ?
DATA ENDS
CODE SEGMENT 
START: MOV AX, DATA
	 MOV DS, AX
	 MOV AL, COUNT
	 INC AL
	 MOV BL, COUNT
	 MUL BL
	 MOV BL, 02
	 DIV BL
	 MOV RES, AX
#	 MUL AX [FOR CUBING]
	 INT 03H
CODE ENDS
END START