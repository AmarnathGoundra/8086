ASSUME CS: CODE, DS: DATA
DATA SEGMENT
A DW 0005H
B DW 0002H
RES DW ?
DATA ENDS
CODE SEGMENT
START: MOV AX, DATA
	 MOV DS, AX
	 MOV SI, OFFSET A
	 MOV AX, [SI]
	 INC SI
	 INC SI
	 ADD AX, [SI]
	 MOV [SI+2], AX
	 MOV AX, [SI-2]
	 SUB AX, [SI]
	 MOV [SI+4], AX
	 MOV AX, [SI-2]
	 MUL WORD PTR [SI]
	 MOV [SI+6], AX
	 MOV [SI+8], DX
	 MOV AX, [SI-2]
	 DIV WORD PTR[SI]
	 MOV [SI+10], AX
	 MOV [SI+12], DX
	 INT 03H
CODE ENDS
END START 