ASSUME CS: CODE, DS: DATA, ES: EXTRA
DATA SEGMENT
A DB "WELCOME$"
STRLEN DB 07H
DATA ENDS
EXTRA SEGMENT
EXTRA ENDS
CODE SEGMENT
START: MOV AX, DATA
	 MOV DS, AX
	 MOV AX, EXTRA
	 MOV ES, AX
	 MOV SI, OFFSET A
	 MOV CL, STRLEN
	 CLD
	 REP MOVSB
	 INT 03H
CODE ENDS
END START