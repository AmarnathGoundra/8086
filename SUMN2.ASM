ASSUME CS: CODE, DS: DATA
DATA SEGMENT
COUNT DB 03H
RES DW ?
DATA ENDS
CODE SEGMENT 
START: MOV AX, DATA
	 MOV DS, AX
	 MOV AL, COUNT
	 INC AL
	 MOV BL, COUNT
	 MUL BL
	 MOV BX, AX
	 MOV AL, COUNT
	 MOV CL, 02
	 MUL CL
	 INC AL
	 MUL BX
	 MOV BL, 06
	 DIV BL
	 MOV RES, AX
	 INT 03H
CODE ENDS
END START