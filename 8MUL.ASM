ASSUME CS: CODE, DS: DATA
DATA SEGMENT
A DB 05H
B DB 02H
RES DW ?
DATA ENDS
CODE SEGMENT
START: MOV AX, DATA
	 MOV DS, AX
  	 MOV AH, A
	 MOV AL, B
	 MUL AH
	 MOV RES, AX
	 INT 03H
	 CODE ENDS
END START