ASSUME CS: CODE, DS: DATA
DATA SEGMENT
A DB 'WELCOMES !!!!!$'
STRLENGTH DB ?
DATA ENDS
CODE SEGMENT
START: MOV AX, DATA
	 MOV DS, AX
	 XOR CL, CL
	 MOV SI, OFFSET A
	 CLD 
      BACK: LODSB
		  INC CL
	  	  CMP AL, '$'
		  JNZ BACK
	 DEC CL
	 MOV STRLENGTH, CL
INT 03H 
CODE ENDS
END START