ASSUME CS: CODE, DS: DATA
DATA SEGMENT
COUNT DB 07
RES DB ?
DATA ENDS
CODE SEGMENT 
START: MOV AX, DATA
	 MOV DS, AX
	 XOR AH, AH
	 MOV AL, COUNT
	 MOV BL, AL
	 CLC
#	 SHR [GETS EXACT VALUE]
	 ROR AL, 01
	 XOR AL, BL
	 MOV RES, AL 
	 INT 03H
CODE ENDS
END START