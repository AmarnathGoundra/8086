ASSUME CS: CODE, DS: DATA
DATA SEGMENT
A DW 0192H
B DW 0181H
RES DW ?
DATA ENDS
CODE SEGMENT
START: MOV AX, DATA
	 MOV DS, AX
	 MOV AX, A
	 MOV BX, B
	 ADD AX, BX
	 # SUB AX, BX
	 MOV RES, AX
	 INT 03H
	 CODE ENDS
END START