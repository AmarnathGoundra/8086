ASSUME CS: CODE, DS: DATA
DATA SEGMENT
A DW 0005H
B DW 0002H
RES DW ?
DATA ENDS
CODE SEGMENT
START: MOV AX, DATA
	 MOV DS, AX
	 MOV SI, OFFSET A
	 MOV DI, OFFSET RES
	 MOV AX, [SI]
	 AND AX, [SI+2]
	 MOV [DI], AX
	 MOV AX, [SI]
	 OR AX, [SI+2]
	 MOV [DI+2], AX
	 MOV AX, [SI]
	 XOR AX, [SI+2]
	 MOV [DI+4], AX
	 MOV AX, [SI]
	 NOT AX
	 MOV [DI+6], AX
	 INT 03H
CODE ENDS
END START 