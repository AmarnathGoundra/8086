ASSUME CS: CODE, DS: DATA
DATA SEGMENT
A DB 84H
B DB 95H
RES DW ?
DATA ENDS
CODE SEGMENT
START: MOV AX, DATA
	 MOV DS, AX
	 XOR AH, AH
	 MOV BL, A
	 MOV AL, B
#	 ADD AL, BL
#	 AAA
	 SUB AL, BL
	 AAS
	 MOV RES, AX
	 INT 03H
	 CODE ENDS
END START